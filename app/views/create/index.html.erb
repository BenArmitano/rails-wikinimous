<h1>All Articles/h1>
<% @articles.each do |article| %>
  <h2><%= t.title %></h2>
  <p><%= t.content %></p>
  <%= link_to "Delete", articles_path(@article),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>

